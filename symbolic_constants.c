#include <stdio.h>

#define hareesh 3000 //this is just a string of characters even though they are numbers

int main()
{
  printf("the value of adding plus one to a define variable is %d", (hareesh + 1));
}
